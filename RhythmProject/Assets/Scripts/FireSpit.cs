using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;


/*
 *  THIS CLASS IS NOT IN USE CURRENTLY
 * */

//public class FireSpit : MonoBehaviour {
//	public static Animator fireAnimationClip;
//
////	public Animator anim;
////	public static bool setFire;
////	private bool isMoving;
//
////	private GameObject fireImage;
////	public static bool initialFire;
//
//	// Use this for initialization
//	void Start () {
////		fireImage = GameObject.FindWithTag ("Fire");
////		initialFire = false;
////		anim = GetComponent<Animator> ();
////		setFire = true;
////		isMoving = false;
//		//anim.SetBool ("spitFire", setFire);
//		fireAnimationClip = GetComponent<Animator> ();
//	}
//	
//	// Update is called once per frame
//	void Update () {
////		if (initialFire) {
////			//fireImage.GetComponent<Image>().canvasRenderer
////			fireImage.GetComponent<Image> ().CrossFadeAlpha (255, 1000, false);
////			//fireImage.transform.position = 
////		}
////		fireImage.CrossFadeAlpha (0, 1, false);
////		if (!isMoving) {
////			anim.SetBool ("spitFire", setFire);
////		}
////		if (anim.GetCurrentAnimatorStateInfo(0).IsName("spitFire"))	{
////			isMoving = true;
////		}
//		//anim.SetBool ("spitFire", setFire);
//		//Debug.Log ("setFire "+ setFire);
//
//	}
//}
